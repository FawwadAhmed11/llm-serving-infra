apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vllm-mock-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: vllm-mock
  endpoints:
    - port: http
      interval: 15s
  namespaceSelector:
    matchNames:
      - default